<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:units="clr-namespace:OpenSilverShowcase.Support.UI.Units">
    
    <Color x:Key="PrimaryBlue">#FF6366F1</Color>
    <Color x:Key="PrimaryPurple">#FF9333EA</Color>
    <Color x:Key="PrimaryCyan">#FF3B82F6</Color>

    <LinearGradientBrush x:Key="BrandGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource PrimaryBlue}" Offset="0"/>
        <GradientStop Color="{StaticResource PrimaryPurple}" Offset="0.5"/>
        <GradientStop Color="{StaticResource PrimaryCyan}" Offset="1"/>
    </LinearGradientBrush>
    
    <Style TargetType="units:ShowcaseListBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="units:ShowcaseListBox">
                    <ItemsPresenter/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="units:ShowcaseListBoxItem">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="units:ShowcaseListBoxItem">
                    <Border x:Name="RootBorder" 
                            Background="Transparent" 
                            BorderBrush="Transparent" 
                            BorderThickness="0 0 0 2"
                            Margin="0 0 10 10"
                            CornerRadius="0">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CustomStates">
                                <VisualState x:Name="CustomNormal"/>
                                <VisualState x:Name="CustomHover">
                                    <Storyboard>
                                        <ColorAnimation Duration="0" 
                                                      To="#000000" 
                                                      Storyboard.TargetName="ContentText" 
                                                      Storyboard.TargetProperty="(TextBlock.Foreground).(SolidColorBrush.Color)"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="CustomPressed">
                                    <Storyboard>
                                        <ColorAnimation Duration="0" 
                                                      To="#F1F1F1" 
                                                      Storyboard.TargetName="RootBorder" 
                                                      Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"/>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="CustomSelected">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Duration="0"
                                     Storyboard.TargetName="RootBorder" 
                                     Storyboard.TargetProperty="BorderBrush">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource BrandGradient}"/>
                                        </ObjectAnimationUsingKeyFrames>

                                        <ObjectAnimationUsingKeyFrames Duration="0"
                                     Storyboard.TargetName="ContentText" 
                                     Storyboard.TargetProperty="Foreground">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource BrandGradient}"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <TextBlock x:Name="ContentText" 
                                   Text="{Binding Name}" 
                                   Padding="0 6 0 4" 
                                   FontSize="12"
                                   FontWeight="Medium"
                                   Foreground="#777777"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>