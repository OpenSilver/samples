<units:ShowcaseItem
    x:Class="OpenSilverShowcase.TransContent.Examples.TransItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:units="clr-namespace:OpenSilverShowcase.Support.UI.Units;assembly=OpenSilverShowcase.Support"
    Description="fundamentals of button styling with colors, fonts, and images."
    Title="Basic Style"
    Order="1">
    <units:ShowcaseItem.CodeSources>
        <units:CodeSource Key="xaml" Source="/Examples/TransItem.xaml"/>
        <units:CodeSource Key="xaml.cs" Source="/Examples/TransItem.xaml.cs"/>
    </units:ShowcaseItem.CodeSources>
    <units:ShowcaseItem.DemoContent>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ContentControl Content="Transitioning ContentControl"
                            Foreground="#FFD972AA" />

            <TransitioningContentControl Name="defaultTCC"
                                         Grid.Row="1"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"
                                         Content="Press buttons to change content"
                                         FontSize="12"/>

            <StackPanel Grid.Row="2"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Center">
                <Button Grid.Column="1"
                        Margin="3"
                        Click="ChangeContentWithDefaultTransition"
                        Content="Default"/>
                <Button Grid.Column="2"
                        Margin="3"
                        Click="ChangeContentWithUpTransition"
                        Content="Up"/>
                <Button Grid.Column="3"
                        Margin="3"
                        Click="ChangeContentWithDownTransition"
                        Content="Down"/>
            </StackPanel>
        </Grid>
    </units:ShowcaseItem.DemoContent>
</units:ShowcaseItem>
