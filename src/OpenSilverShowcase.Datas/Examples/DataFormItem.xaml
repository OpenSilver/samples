<units:ShowcaseItem
    x:Class="OpenSilverShowcase.Datas.Examples.DataFormItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:units="clr-namespace:OpenSilverShowcase.Support.UI.Units;assembly=OpenSilverShowcase.Support"
    HorizontalContentAlignment="Stretch"
    Description="fundamentals of button styling with colors, fonts, and images."
    Title="DataForm"
    Order="1">
    <units:ShowcaseItem.CodeSources>
        <units:CodeSource Key="xaml" Source="/Examples/DataFormItem.xaml"/>
        <units:CodeSource Key="xaml.cs" Source="/Examples/DataFormItem.xaml.cs"/>
        <units:CodeSource Key="converter" Source="/Converters/EnumToListConverter.xaml.cs"/>
        <units:CodeSource Key="model" Source="/Models/Planet.cs"/>
    </units:ShowcaseItem.CodeSources>
    <units:ShowcaseItem.DemoContent>
        <Grid xmlns:cnvts="clr-namespace:OpenSilverShowcase.Datas.Converters"
              xmlns:model="clr-namespace:OpenSilverShowcase.Datas.Models">
            <Grid.Resources>
                <cnvts:EnumToListConverter x:Key="EnumToListConverter"/>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <DataForm x:Name="DataForm1" 
                      Grid.Row="0"
                      Margin="10"/>

            <StackPanel Grid.Row="1" Margin="10">
                <TextBlock Text="CommandButtonsVisibility" 
                           Margin="0,5,0,5"
                           FontWeight="Bold"/>

                <ComboBox SelectedItem="{Binding CommandButtonsVisibility, 
                          ElementName=DataForm1, 
                          Mode=TwoWay}"
                          ItemsSource="{Binding Converter={StaticResource EnumToListConverter}, 
                          ConverterParameter={x:Static DataForm.CommandButtonsVisibilityProperty}}"
                          FontSize="12" 
                          Margin="0,0,0,10"/>
            </StackPanel>
        </Grid>
    </units:ShowcaseItem.DemoContent>
</units:ShowcaseItem>
